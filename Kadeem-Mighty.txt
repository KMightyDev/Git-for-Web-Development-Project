Markup/stylying project: https://codepen.io/MightyK/full/xxGpvyP
   
    1. What is Semantic HTML? 
        -Semantic HTML is HTML that gives meaning to our content such as <nav>, <section>, <footer>,
        & <header>.

    2. What is HTML used for? 
        -HTML is used to markup the bodies of text on a webpage.

    3. What is an attribute and where do we put it?
        -An attribute i used to modify an HTML element. Attributes are strategically placed within
        the safe confines of the HTML tag.

    4. What is the h1 tag used for? How many times should I use it on a page?
        -The <h1> is used to define importance amongt headlines. <h1>'s are generally used once 
        per page.

    5. Name two tags that have required attributes
        -The <img> & <a>(anchor tag) require attributes.

    6. What do we put in the head of our HTML document?
        -Metadata(Document title, styles, scripts)

    7. What is an id? 
        -An id is an attribute used to point to a style sheet.

    8. What elements can I add an id to? 
        -All HTML elements

    9. How many times can I use the same id on a page?
        -One time. 

    10. What is a class? 
        -An attribute that can be used to specify one or more class names.

    11. What elements can I add a class to?
        -All HTML elements.

    12. How many times can I use the same class on a page? 
        -Multiple intances.

    13. How do I get my link to open in a new tab?
        - By using the "target='_blank'" attribute.

    14. What is the alt attribute used for? 
        -Used to specify alternative text.

    15. How do I reference an id?
        -An id is referenced by using the hash(#) symbol.

    16. What is the difference between a section and a div
        -Section is used for semantic definition whilst the div is generally used for css hooks.

    17. What is CSS used for? 
        -CSS is used to style a webpage.

    18. How to we select an element? Example - every h2 on the page
        -You can selct every h2 by calling it as such: "h2{

        }"

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
        -A class is able to specify muliple elements on a page while and id can only specify one.
        You may want to use a class to create a box model for your content. You may want to use an id
        when you want to override the specific changes to one element as it trumps in specificity.

    20. How do we select classes in CSS?
        -Classes are selected using the ".className"

    21. How do we select a p element with a single class of “human””?
        -".human p" would select the p element within the class of "human"

    22. What is a parent child selector? When would this be useful?
        A parent child selector is used to select elements nested within other elements.
        This is useful when working on large, complex, bodies of code.

    23. How do you select all links within a div with the class of sidebar? 
        -".sidebar div"

    24. What is a pseudo selector?
        -Allows us to select something that cannot be expressed by a normal selector (i.e ":hover")
    
    25. What do we use the change the spacing between lines?
        -We can use the line height property to adjust the spacing between lines.

    26. What do we use to change the spacing between letters?
        -We can use the letter-spacing property to change the spacing between letters.

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
        -We can use the text-transform property to change text between CAPITALS and lowercase.

    28. How do I add a 1px border around my div that is dotted and black?
        -".clasName {
            border: 1px dotted black
        }"
    29. How do I select everything on the page?
        -With the everything(*) selector.

    30. How do I write a comment in CSS?
        -"/* */"

    31. How do I find out what file I am in, when I am using the command line? 
        -"
        pwd"
    32. Using the command line - how do I see a list of files/folders in my current folder?
        -By using the 'ls' command

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
        -By using the "rm" command. You must be careful when using this becase it's irreversable
        as well as provides no propts or warnings.

    34. Why should I use version control?
        -The better track my work as well as to be able to "time travel" through my work. Versin control
        allows us to revert to an earlier version in case we manage to break our code.

    35. How often should I commit to github?
        -As long as you're working any interval of time between 15 and 30 minutes.

    36. What is the command we would use to push our repo up to github? 
        -"git push -u origin branchName"

    37. Walk me through Lambda's git flow.
       -1.) Fork.
        2.) Add collborators.
        3.) Use "cd" to enter the directory where you'd like your clone.
        4.) Clone the repository to the specified directory.
        5.) Use "cd clonedRepoName" to enter your new repository. 
        6.) Use "git checkout -b 'firstname-lastname'" to create your branch name for this repo.
        7.) Add, commit, and push using the "git add ." "git commit -m 'Place commit message here'"
            and "git push -u origin branchName"
        8.) Submit pull request.
        9.) Add TL/PM as a reviewer.

Stretch Questions

    1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the fix for the box model? 



   --> 